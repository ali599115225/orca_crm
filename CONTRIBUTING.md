# المساهمة في ORCA

هذا المستودع يخضع لنطاقات عمل محكومة، وبوابات تحقق إلزامية، وفصل صريح بين التخطيط والتنفيذ والنشر.

## 1. قبل البدء

- حدّد النتيجة المطلوبة، الملفات المسموح تعديلها، وما هو خارج النطاق.
- اربط التغيير بمتطلب أو فجوة أو بوابة أو بلاغ عطل محدد.
- افحص التغييرات المتوازية لتجنب التعارض والتكرار.
- لا تبدأ تنفيذ UI دون مرجع بصري مستقل معتمد للصفحة أو التبويب أو النافذة نفسها.
- لا تعتبر مستندًا تخطيطيًا دليلًا على أن Runtime الحالي مطابق.

## 2. الفروع والنطاق

- لا تعدّل `main` مباشرة.
- استخدم فرعًا محدودًا باسم يوضح النطاق، مثل:
  - `work/<scope>-<date>`
  - `fix/<issue>-<date>`
  - `docs/<gate>-<date>`
- يجب أن يظل كل Pull Request مستقلًا وقابلًا للمراجعة والتراجع.
- لا تخلط تغييرات التوثيق أو الأدوات أو Runtime أو UI أو Schema في PR واحد إلا عند وجود اعتماد تقني مثبت.
- يمنع Force Push أو إعادة كتابة التاريخ إلا بتفويض صريح ومحدد.

## 3. الحدود عالية المخاطر

تحتاج الإجراءات التالية تفويضًا مستقلًا ودقيقًا قبل التنفيذ:

- الدمج إلى `main`؛
- أي نشر أو تغيير في Production؛
- Schema أو Migration أو Backfill أو حذف أو تصحيح بيانات؛
- إنشاء أو تدوير أو استخدام أسرار وبيانات اعتماد؛
- تفعيل مزود أو Webhook أو خدمة مدفوعة؛
- إرسال رسائل أو مدفوعات أو توقيعات أو إعلانات حقيقية؛
- أي ادعاء قانوني أو تنظيمي دون دليل المالك والمستشار المختص.

عند غياب التفويض أو الدليل، يبقى المسار الآمن معطلًا أو `NOT_CONFIGURED`.

## 4. إعداد البيئة

المتطلبات الأساسية:

- Node.js `24.x`؛
- تثبيت الاعتماديات من Lockfile الموجود؛
- متغيرات بيئة محلية آمنة فقط؛
- عدم استخدام بيانات Production في التطوير أو الاختبار.

```bash
npm ci
```

لا تستخدم `npm install` لتحديث Lockfile ضمن مهمة لا تتطلب تغيير الاعتماديات.

## 5. أوامر التحقق

شغّل الحد الأدنى المناسب لنطاق التغيير:

```bash
npm run quality:text
npm run typecheck
npm test
npm run build
```

الاختبارات المتخصصة المتاحة:

```bash
npm run test:smoke
npm run test:acceptance
npm run test:whatsapp
npm run test:sentinel
npm run security:audit
```

قد تتطلب بوابات المستودع اختبارات أو أدلة إضافية حسب النطاق. نجاح اختبار عام لا يعوض اختبار المتطلب المباشر أو الاختبار السلبي للصلاحيات والعزل.

## 6. معايير الكود والملفات

- استخدم UTF-8 ونهايات أسطر LF وفق `.editorconfig` و`.gitattributes`.
- لا تضف Dependencies جديدة دون ضرورة مثبتة ومراجعة أثر الأمان والتكلفة.
- لا تسجل أسرارًا أو Tokens أو Cookies أو بيانات شخصية أو محتوى مستندات حساسة.
- لا تعرض معرفات تقنية داخل واجهة المستخدم عندما توجد قيمة بشرية آمنة.
- حافظ على فشل آمن: Timeout أو حالة مزود مجهولة لا تتحول إلى نجاح.
- حافظ على عزل نطاق الشركة والصلاحيات من جهة الخادم؛ إخفاء زر في الواجهة ليس تفويضًا.
- أضف اختبارات إيجابية وسلبية وحدودية لكل سلوك متغير.

## 7. التوثيق والأدلة

يجب أن يوضح كل تغيير:

- لماذا يلزم؛
- ما الذي تغير وما الذي لم يتغير؛
- المخاطر والاعتماديات؛
- الاختبارات والأدلة؛
- التراجع أو Forward-fix عند تعذر التراجع؛
- أي قرارات مالك ما زالت مفتوحة.

لا تستخدم Screenshots أو تقارير قديمة لإثبات نجاح رأس Commit مختلف.

## 8. Pull Request

استخدم قالب PR الموجود في `.github/pull_request_template.md` وأكمل جميع الأقسام ذات الصلة.

قبل طلب المراجعة:

- [ ] الفرق النهائي مطابق لقائمة الملفات المسموح بها.
- [ ] لا توجد ملفات مؤقتة أو مولدة غير مقصودة.
- [ ] تم تشغيل فحوص النطاق وتوثيق نتائجها.
- [ ] لا توجد أسرار أو بيانات حساسة في Diff أو Logs أو Screenshots.
- [ ] المخاطر والتراجع والحالة المتبقية موثقة.
- [ ] جميع الفحوص الإلزامية خضراء على الرأس النهائي نفسه.

الفحوص الحمراء أو المعلقة أو المتخطاة أو التي تتطلب إجراء تمنع الدمج.

## 9. البلاغات الأمنية والأعطال

- للثغرات الأمنية أو تسريب الأسرار أو تجاوز الصلاحيات: اتبع `SECURITY.md` ولا تفتح Issue عامًا.
- للأعطال الوظيفية: استخدم نموذج `بلاغ عطل` مع خطوات إعادة إنتاج وأدلة منزوعة الحساسية.

## 10. تعريف الإغلاق

لا يعتبر العمل مغلقًا حتى:

- تتحقق النتيجة المطلوبة فعليًا؛
- تنجح الاختبارات والبوابات على الرأس النهائي؛
- تُراجع المخاطر والحالة المتبقية؛
- تزال الملفات والتصاريح المؤقتة أو يوثق سبب بقائها؛
- تتم المصالحة مع أي حالة أعمال أو مزود تأثرت بالتغيير؛
- يصدر اعتماد المالك عندما يكون مطلوبًا.

> الصمت أو الحقول الفارغة أو نجاح البناء وحده لا تعني الموافقة أو اكتمال التغيير.
